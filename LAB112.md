# Rate Limiting Bypass - Request Limit Environment

## Summary

I identified a Rate Limiting Bypass vulnerability in the request limit environment at `koko-limit.kzlabs.in`.

The application is designed to test request limiting mechanisms but fails to properly enforce restrictions on the number of requests per session or IP address. By using a brute-force attack with sequential numeric payloads (0 through 7), an attacker can send multiple requests to the server without being blocked or rate-limited. All requests return a `200 OK` status code (except one returning `300`), confirming that the limiting mechanism is either not implemented or easily bypassed.

This vulnerability allows attackers to flood the server with requests, potentially leading to denial of service (DoS), resource exhaustion, or bypass of security controls.

## Vulnerability Type

**Rate Limiting Bypass / Request Limit Abuse**

## Vulnerable Parameter

```text
Payload (numeric values)
```

## Vulnerable URL

```text
https://koko-limit.kzlabs.in/index.php
```

## Steps to Reproduce

1. Navigate to the vulnerable request limiting page.

2. Intercept a request using Burp Suite, OWASP ZAP, or a similar proxy tool.

3. Send the request to Intruder (Burp Suite) or similar brute-force tool.

4. Configure the attack with sequential numeric payloads (e.g., 0, 1, 2, 3, 4, 5, 6, 7).

5. Launch the attack.

6. Observe that all requests return a `200 OK` or `300` status code, indicating that rate limiting is not being enforced.

## Payloads Used

```text
Numeric: 0, 1, 2, 3, 4, 5, 6, 7
```

## Proof of Concept

The application sends a request to the server with a payload parameter:

```http
POST /index.php HTTP/2
Host: koko-limit.kzlabs.in
Content-Type: application/x-www-form-urlencoded

payload=0
```

The server responds with a `200 OK` status code for most requests:

```http
HTTP/2 200 OK
Date: Tue, 25 Aug 2026 16:03:48 GMT
Content-Type: text/html; charset=UTF-8
Server: cloudflare
X-Powered-By: PHP/8.2.33
```

All 8 requests in the attack returned `200 OK` or `300`:

| Request | Payload | Status Code | Length |
|---|---|---|---|
| 0 | 0 | 200 | 10834 |
| 1 | 1 | 200 | 10829 |
| 2 | 2 | 200 | 10829 |
| 3 | 3 | 200 | 10825 |
| 4 | 4 | 200 | 10831 |
| 5 | 5 | 200 | 10825 |
| 6 | 6 | 200 | 10827 |
| 7 | 7 | 300 | 4507 |

This confirms that the rate limiting mechanism is either not implemented, inconsistently applied, or easily bypassed.

<img width="1348" height="680" alt="Screenshot 2026-08-25 213048" src="https://github.com/user-attachments/assets/d4a9b089-9bc7-4a33-a428-8f3288546334" />
<img width="1912" height="973" alt="Screenshot 2026-08-25 213103" src="https://github.com/user-attachments/assets/c263c056-0dc0-488a-866b-108cbaf361bf" />

## Impact

This Rate Limiting Bypass vulnerability allows an attacker to:

- Perform denial of service (DoS) attacks on the server
- Exhaust server resources (CPU, memory, database connections)
- Flood the application with malicious requests
- Bypass anti-brute-force protections
- Potentially discover other vulnerabilities through fuzzing
- Consume bandwidth and network resources

## Recommendation

To mitigate this vulnerability:

### Server-Side Rate Limiting
```php
$ip = $_SERVER['REMOTE_ADDR'];
$sessionId = session_id();
$key = "rate_limit:$ip:$sessionId";
$attempts = redis_get($key);
if ($attempts > 10) {
    http_response_code(429);
    return "Too many requests. Please try again later.";
}
redis_incr($key, 60); // Increment and expire after 1 minute
```

### IP-Based Throttling
```php
$ip = $_SERVER['REMOTE_ADDR'];
$key = "ip_limit:$ip";
$attempts = redis_get($key);
if ($attempts > 20) {
    http_response_code(429);
    return "Too many requests from this IP.";
}
redis_incr($key, 60); // Increment and expire after 1 minute
```

### Session-Based Throttling
```php
$sessionId = session_id();
$key = "session_limit:$sessionId";
$attempts = redis_get($key);
if ($attempts > 15) {
    http_response_code(429);
    return "Too many requests for this session.";
}
redis_incr($key, 60); // Increment and expire after 1 minute
```

### Additional Recommendations

- **Request Validation**: Validate request parameters to filter out malicious payloads.
- **Logging and Monitoring**: Log all requests and monitor for anomalies.
- **Web Application Firewall (WAF)**: Deploy a WAF to detect and block request flooding.
- **Rate Limiting Headers**: Implement `X-RateLimit-*` headers to inform clients of limits.
- **CAPTCHA Implementation**: Implement CAPTCHA after a certain number of requests.
- **Exponential Backoff**: Implement increasing delays between requests from the same source.
- **Connection Limits**: Limit the number of concurrent connections from a single IP.
- **Regular Security Audits**: Conduct regular assessments to identify and fix vulnerabilities.
- **Intrusion Detection System (IDS)**: Deploy IDS to detect abnormal traffic patterns.
- **Load Balancer Configuration**: Configure load balancers to distribute traffic and prevent overload.

---
